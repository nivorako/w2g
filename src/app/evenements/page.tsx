export const metadata = { title: "Événements | We are together" };

/**
 * Page component.
 *
 * Renders the main content for the events page.
 *
 * @returns {React.ReactElement} The page content.
 */
export default function Page() {
    return (
        <main style={{ maxWidth: 1200, margin: "1.25rem auto 2rem", padding: "0 1rem" }}>
            <h1>Événements</h1>
        </main>
    );
}
